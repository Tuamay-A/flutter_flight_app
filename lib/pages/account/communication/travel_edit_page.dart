import 'package:flutter/material.dart';

class TravelEditPage extends StatelessWidget {
  const TravelEditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("The Travel Edit")),
      body: Center(child: Text("Travel edit settings here")),
    );
  }
}
