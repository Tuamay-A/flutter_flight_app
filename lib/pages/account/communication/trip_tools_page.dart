import 'package:flutter/material.dart';

class TripToolsPage extends StatelessWidget {
  const TripToolsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(title: Text("Trip Tools & Features")),
      body: Center(child: Text("Trip tools settings here")),
    );
  }
}
