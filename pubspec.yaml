# name: expedia
# description: "A new Flutter project."

# version: 1.0.0+1

# environment:
#   sdk: ^3.10.3

# dependencies:
#   flutter:
#     sdk: flutter
  
#   # Already installed - Keep these
#   get: ^4.6.6
#   shared_preferences: ^2.5.4
#   image_picker: ^1.0.7
#   intl: ^0.20.2
#   http: ^1.6.0
#   cupertino_icons: ^1.0.8
#   dio: ^5.9.2
#   flutter_secure_storage: ^10.0.0
  
#   # API Integration Dependencies - All Compatible Versions
#   # JSON Serialization & Code Generation
#   logger: ^2.5.0
#   retrofit: ^4.0.1           # Stable version
#   json_annotation: ^4.8.1    # Compatible with retrofit 4.0.1
#   freezed_annotation: ^2.4.1  # Compatible version
  
#   # Connectivity & Utilities
#   connectivity_plus: ^6.1.3
#   equatable: ^2.0.7
  
#   # UI Enhancements
#   shimmer: ^3.0.0
#   another_flushbar: ^1.12.30
#   pull_to_refresh: ^2.0.0

# dev_dependencies:
#   flutter_test:
#     sdk: flutter

#   # Already installed - Keep these
#   flutter_launcher_icons: ^0.14.4
#   flutter_lints: ^6.0.0
  
#   # Code Generation Dependencies - Compatible with above versions
#   build_runner: ^2.4.8       # Compatible version
#   json_serializable: ^6.7.1  # Compatible with json_annotation 4.8.1
#   freezed: ^2.4.5            # Compatible with freezed_annotation 2.4.1
#   retrofit_generator: ^7.0.8  # Compatible with retrofit 4.0.1

# flutter_icons:
#   android: true
#   ios: true
#   image_path: "assets/images/EthiopianAirlineLogo.png"

# flutter:
#   uses-material-design: true

#   assets:
#     - assets/images/
name: expedia
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  shared_preferences: ^2.5.4
  image_picker: ^1.0.7
  intl: ^0.20.2
  http: ^1.6.0
  device_preview: ^1.2.0
  

  cupertino_icons: ^1.0.8
  dio: ^5.9.2
  flutter_secure_storage: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_launcher_icons: ^0.14.4
  flutter_lints: ^6.0.0
flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/EthiopianAirlineLogo.png"

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/data/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
